import React from 'react'
import {Text} from 'react-native'

const ToneEditComponent: React.FC = () => {

    return(
        <Text> This is the Edit component</Text>
    )
}

export default ToneEditComponent;